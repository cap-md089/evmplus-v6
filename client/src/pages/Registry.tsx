import * as React from 'react';
import Page, { PageProps } from './Page';

export default class RegEdit extends Page<PageProps, {}> {
	public state: {} = {};

	public render() {
		return <div />;
	}
}